# Google Maps JavaScript API (branch map on /branches). Enable "Maps JavaScript API" in Google Cloud Console.
# Copy to .env.local and paste your key (never commit real keys).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
